import { TikiService } from '../src/service/tiki-service'
import config from './__fixtures__/config'

describe("Full month tests", () => {
  test("Start balance $0. After all requirements fulfilled, $1 balance", () => {
     const tikiService = new TikiService(config)
  })
});
